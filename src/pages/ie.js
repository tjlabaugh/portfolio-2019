import React from 'react';
import Layout from '../components/Layout';
import IE from '../components/IE';

const ie = () => {
  return (
    <Layout>
      <IE />
    </Layout>
  );
};

export default ie;
